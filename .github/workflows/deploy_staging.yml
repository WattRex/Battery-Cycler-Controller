# This workflow deploys battery cycler controller to development environment

name: Deployment of battery cycler controller to staging environment

on:
  pull_request_review:
    review:
      state: 
        - approved
    pull_request:
      head:
        ref: [ "mid_data", "mid_meas", "mid_dabs", "mid_str",
                "mid_pwr", "app_diag", "app_man" ]

env:
  PACKAGE_NAME: wattrex-battery-cycler-controller

jobs:
  # build-docker-image:
  #   name: Build docker image
  #   if: ${{ github.event.review.state == 'approved' }}
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v4
  #     - name: Set up Docker Buildx
  #       uses: docker/setup-buildx-action@v3
  #     - name: Build and export
  #       uses: docker/build-push-action@v5
  #       with:
  #         context: .
  #         file: ./devops/docker/Dockerfile.cycler
  #         tags: wattrex-cycler:latest
  #         outputs: type=docker,dest=/tmp/wattrex-cycler.tar
  #         push: false
  test-job:
    name: Test cycler controller
    runs-on: ubuntu-latest
    steps:
      - name: Echo info
        run: |
          echo "This is a test job"
          echo "Pull request head ref: ${{ github.event.pull_request.head.ref }}"
          echo "Pull request base ref: ${{ github.event.pull_request.base.ref }}"
          echo "Pull request head sha: ${{ github.event.pull_request.head.sha }}"
          echo "Pull request base sha: ${{ github.event.pull_request.base.sha }}"
          echo "Pull request number: ${{ github.event.pull_request.number }}"
          echo "Pull request title: ${{ github.event.pull_request.title }}"
          echo "Pull request body: ${{ github.event.pull_request.body }}"
          echo "Pull request review state: ${{ github.event.review.state }}"
          echo "Pull request review body: ${{ github.event.review.body }}"
          echo "Pull request review user: ${{ github.event.review.user }}"
          echo "Pull request review submitted_at: ${{ github.event.review.submitted_at }}"
          echo "Pull request review commit_id: ${{ github.event.review.commit_id }}"
          echo "Pull request review html_url: ${{ github.event.review.html_url }}"
          echo "Pull request review pull_request_url: ${{ github.event.review.pull_request_url }}"
          echo "Pull request review author_association: ${{ github.event.review.author_association }}"
          echo "Pull request review id: ${{ github.event.review.id }}"
          echo "Pull request review node_id: ${{ github.event.review.node_id }}"
      

  launch-real-bat:
    name: Launch cycler controller in real environment
    # needs: build-docker-image
    uses: ./.github/workflows/staging_deploy_real_bat.yml
    secrets: inherit
